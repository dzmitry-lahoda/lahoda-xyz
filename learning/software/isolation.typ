

namespaces

capabilities

priveleged process with user id 0 (root)
  privileges split into capabilities
    per thread
 
unpriovileged non zero


seccom-bpf

namespaces

control groups

isolate:
 -- network stack
 -  process table
- mount table