module SpatialPoolingSupportingRoutines
open State
open System.Collections.Generic
let overlap(column:column) =
    (1.0)


let connectedSynapses(column:column) =
    (new List<synapse>())

let input (t:float) (sourceInput:float) = 
    (1.0)

let boost(column:column) =
    (1.0)

    