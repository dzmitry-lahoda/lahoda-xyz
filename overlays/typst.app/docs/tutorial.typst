

There are `mode`s and there are `function`.

Some functions can work only in specific modes.

Some function have special symbols.

Special mode is math mode. 

Elements are symbols and functions.

Elements need `#` prefix before elements.
In math mode no need for such prefix.


There are symbols. Some symbols has variants accesible via `.`.