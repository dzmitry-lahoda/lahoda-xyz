
Nixos "install" have many flavors.

Main properties are:
- is it built locally or on remote
- is it uses axisting storage layout or formats to new one
- it is installed from boot or from other OS
- does it requires interaction or unattended
- does it discovers hardware config automatically or needs manual tuning
- does it works with flakes or needs some glue to flake
