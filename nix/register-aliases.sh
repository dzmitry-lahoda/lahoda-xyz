# Register aliases for 
# - most frequently used
# - for option which is long to type, but shows information in most concise and yet full way (including tree grouping)
alias show-storage='lsblk --fs --paths --merge --tree'